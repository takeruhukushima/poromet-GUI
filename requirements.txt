fastapi==0.104.1
mangum==0.17.0
python-multipart==0.0.6
uvicorn==0.24.0
numpy==1.26.4
scikit-image==0.22.0
porespy==1.5.0  # Using an older version that has pre-built wheels
matplotlib==3.8.2
pillow==10.1.0
pydantic==2.6.1
--only-binary=:all:
# Exclude problematic dependencies
--no-deps  # Prevent automatic dependency resolution
--find-links https://pypi.org/simple/  # Use PyPI as source
